stop/start container

add many users
fix times should be the same in container and on host
allow adding files
create frontend only for browsing without modification options

done:
fix interface in files (show file content)
download container history and repo
online interface for history and commits browsing
